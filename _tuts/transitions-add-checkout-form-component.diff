diff --git a/assets/pages/shopping-cart.vue b/assets/pages/shopping-cart.vue
index 7970c1d11..9ec5dde0c 100644
--- a/assets/pages/shopping-cart.vue
+++ b/assets/pages/shopping-cart.vue
@@ -10,12 +10,16 @@
                     <loading v-show="completeCart === null" />
 
                     <shopping-cart-list
-                        v-if="completeCart"
+                        v-if="completeCart && currentState === 'cart'"
                         :items="completeCart.items"
                         @updateQuantity="updateQuantity"
                         @removeFromCart="removeFromCart"
                     />
 
+                    <checkout-form
+                        v-if="completeCart && currentState === 'checkout'"
+                    />
+
                     <div
                         v-if="completeCart && completeCart.items.length > 0"
                         class="text-right"
@@ -36,6 +40,7 @@
 <script>
 import { fetchFullCart, updateCartItemQuantity, removeItemFromCart } from '@/services/cart-service';
 import ShoppingCartMixin from '@/mixins/get-shopping-cart';
+import CheckoutForm from '@/components/checkout';
 import TitleComponent from '@/components/title';
 import ShoppingCartList from '@/components/shopping-cart';
 import Loading from '@/components/loading';
@@ -43,6 +48,7 @@ import Loading from '@/components/loading';
 export default {
     name: 'ShoppingCart',
     components: {
+        CheckoutForm,
         Loading,
         ShoppingCartList,
         TitleComponent,
