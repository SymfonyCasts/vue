diff --git a/assets/pages/shopping-cart.vue b/assets/pages/shopping-cart.vue
index bfbfba5da..f228d9c5e 100644
--- a/assets/pages/shopping-cart.vue
+++ b/assets/pages/shopping-cart.vue
@@ -44,7 +44,16 @@ export default {
             const productIds = this.cart.items.map((item) => item.product);
 
             const productsResponse = await fetchProductsById(productIds);
-            console.log(productsResponse, this.cart);
+            const completeItems = productsResponse.data['hydra:member'].map((product) => {
+                const cartItem = this.cart.items.find((item) => item.product === product['@id']);
+
+                return {
+                    product,
+                    color: cartItem.color,
+                    quantity: cartItem.quantity,
+                };
+            });
+            console.log(completeItems);
         },
     },
 };
