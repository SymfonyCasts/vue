diff --git a/assets/components/sidebar.vue b/assets/components/sidebar.vue
index b1ace471..0ea87332 100644
--- a/assets/components/sidebar.vue
+++ b/assets/components/sidebar.vue
@@ -51,22 +51,13 @@
 <script>
 import Loading from '@/components/loading';
 import useCollapsibleSidebar from '@/modules/collapsible-sidebar';
+import usePageContext from '@/modules/page-context';
 
 export default {
     name: 'Sidebar',
     components: {
         Loading,
     },
-    props: {
-        currentCategoryId: {
-            type: String,
-            default: null,
-        },
-        categories: {
-            type: Array,
-            required: true,
-        },
-    },
     computed: {
         loading() {
             return this.categories.length === 0;
@@ -74,8 +65,13 @@ export default {
     },
     setup() {
         const { sidebarCollapsed, toggleSidebarCollapsed } = useCollapsibleSidebar();
+        const { currentCategoryId, categories, loadCategories } = usePageContext();
+
+        loadCategories();
 
         return {
+            categories,
+            currentCategoryId,
             sidebarCollapsed,
             toggleSidebarCollapsed,
         };
