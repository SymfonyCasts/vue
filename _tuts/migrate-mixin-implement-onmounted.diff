diff --git a/assets/js/factory/get-shopping-cart.js b/assets/js/factory/get-shopping-cart.js
index b53c532c..e1555979 100644
--- a/assets/js/factory/get-shopping-cart.js
+++ b/assets/js/factory/get-shopping-cart.js
@@ -8,13 +8,20 @@ const cart = reactive({
 
 export default function useShoppingCart() {
     onMounted(async () => {
+        cart.items.length = 0;
+
+        let newCart;
         try {
-            this.cart = await getCart();
+            newCart = await getCart();
         } catch (e) {
             return;
         }
 
-        this.updateShoppingCartHeader();
+        newCart.items.forEach((item) => {
+            cart.items.push(item);
+        });
+
+        // this.updateShoppingCartHeader();
     });
 
     return {
@@ -22,15 +29,6 @@ export default function useShoppingCart() {
     };
 
     /*
-        async mounted() {
-            try {
-                this.cart = await getCart();
-            } catch (e) {
-                return;
-            }
-
-            this.updateShoppingCartHeader();
-        },
         methods: {
             updateShoppingCartHeader() {
                 document.getElementById('js-shopping-cart-items')
