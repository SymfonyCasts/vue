diff --git a/assets/js/factory/get-page-shopping-cart.js b/assets/js/factory/get-page-shopping-cart.js
index f231172c..aa7480bf 100644
--- a/assets/js/factory/get-page-shopping-cart.js
+++ b/assets/js/factory/get-page-shopping-cart.js
@@ -4,6 +4,26 @@ export default function usePageShoppingCart() {
     const items = reactive([]);
     const loading = ref(false);
 
+    watch: {
+        'cart.items.length': async function watchCartItemsLength() {
+            this.loading = true;
+            this.items = [];
+
+            if (!this.cart.items.length) {
+                this.loading = false;
+                return;
+            }
+
+            try {
+                this.items = await getFullShoppingCart(this.cart);
+            } catch (e) {
+                this.loading = false;
+            }
+
+            this.loading = false;
+        },
+    },
+
     return {
         items,
         loading,
diff --git a/assets/js/pages/shopping-cart.vue b/assets/js/pages/shopping-cart.vue
index 8e8302d1..1ce3bbac 100644
--- a/assets/js/pages/shopping-cart.vue
+++ b/assets/js/pages/shopping-cart.vue
@@ -96,25 +96,6 @@ export default {
                 : '<< Back';
         },
     },
-    watch: {
-        'cart.items.length': async function watchCartItemsLength() {
-            this.loading = true;
-            this.items = [];
-
-            if (!this.cart.items.length) {
-                this.loading = false;
-                return;
-            }
-
-            try {
-                this.items = await getFullShoppingCart(this.cart);
-            } catch (e) {
-                this.loading = false;
-            }
-
-            this.loading = false;
-        },
-    },
     methods: {
         switchState() {
             this.currentState = this.currentState === 'cart' ? 'checkout' : 'cart';
