diff --git a/assets/components/catalog.vue b/assets/components/catalog.vue
index 79b8a45d..5a215471 100644
--- a/assets/components/catalog.vue
+++ b/assets/components/catalog.vue
@@ -26,6 +26,7 @@ import LegendComponent from '@/components/legend';
 import ProductList from '@/components/product-list';
 import SearchBar from '@/components/search-bar';
 import TitleComponent from '@/components/title';
+import usePageContext from '@/modules/page-context';
 
 export default {
     name: 'Catalog',
@@ -35,16 +36,6 @@ export default {
         SearchBar,
         TitleComponent,
     },
-    props: {
-        currentCategoryId: {
-            type: String,
-            default: null,
-        },
-        categories: {
-            type: Object,
-            required: true,
-        },
-    },
     data() {
         return {
             products: [],
@@ -59,7 +50,7 @@ export default {
                 return 'All Products';
             }
 
-            const category = this.categories.value.find((cat) => (cat['@id'] === this.currentCategoryId));
+            const category = this.categories.find((cat) => (cat['@id'] === this.currentCategoryId));
 
             return category ? category.name : '';
         },
@@ -100,5 +91,13 @@ export default {
             this.products = response.data['hydra:member'];
         },
     },
+    setup() {
+        const { currentCategoryId, categories } = usePageContext();
+
+        return {
+            currentCategoryId,
+            categories,
+        };
+    },
 };
 </script>
