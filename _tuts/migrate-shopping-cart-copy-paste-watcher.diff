diff --git a/assets/js/pages/shopping-cart.vue b/assets/js/pages/shopping-cart.vue
index be87e8be..8df10ef8 100644
--- a/assets/js/pages/shopping-cart.vue
+++ b/assets/js/pages/shopping-cart.vue
@@ -96,25 +96,6 @@ export default {
                 : '<< Back';
         },
     },
-    watch: {
-        'cart.items.length': async function watchCartItemsLength() {
-            this.loading = true;
-            this.fullCartItems.items = [];
-
-            if (!this.cart.items.length) {
-                this.loading = false;
-                return;
-            }
-
-            try {
-                this.fullCartItems.items = await getFullShoppingCart(this.cart);
-            } catch (e) {
-                this.loading = false;
-            }
-
-            this.loading = false;
-        },
-    },
     methods: {
         switchState() {
             this.currentState = this.currentState === 'cart' ? 'checkout' : 'cart';
@@ -131,6 +112,26 @@ export default {
             fullCartItems,
         } = useShoppingCart();
 
+        watch: {
+            'cart.items.length': async function watchCartItemsLength() {
+                this.loading = true;
+                this.fullCartItems.items = [];
+
+                if (!this.cart.items.length) {
+                    this.loading = false;
+                    return;
+                }
+
+                try {
+                    this.fullCartItems.items = await getFullShoppingCart(this.cart);
+                } catch (e) {
+                    this.loading = false;
+                }
+
+                this.loading = false;
+            },
+        },
+
         onMounted(() => {
             loadShoppingCart();
         });
